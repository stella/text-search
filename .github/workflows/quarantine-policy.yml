name: Package quarantine policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce package quarantine
    if: github.repository == 'stella/text-search'
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/quarantine-policy.yml@fb1e83fbc3fae39902da763ca7b2613090571717
